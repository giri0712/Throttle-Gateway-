# ThrottleGate Application Properties
spring.application.name=throttle-gate
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/throttlegate
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Redis/Valkey Configuration
spring.redis.host=localhost
spring.redis.port=6379

# Spring Cloud Config (for hot-reload)
spring.cloud.config.enabled=true
spring.cloud.config.import=optional:configserver:
spring.cloud.config.uri=http://localhost:8888

# ThrottleGate Specific Configuration
throttlegate.algorithm=token-bucket
throttlegate.window-size-seconds=60
throttlegate.default-limits[free:/events]=100
throttlegate.default-limits[free:/payments]=10
throttlegate.default-limits[free:/default]=50
throttlegate.default-limits[pro:/events]=1000
throttlegate.default-limits[pro:/payments]=100
throttlegate.default-limits[pro:/default]=500

# Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics,prometheus,refresh
management.endpoint.health.show-details=always

# Logging
logging.level.com.throttlegate=INFO
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG
